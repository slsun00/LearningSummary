## 下载网页

```go
// 下载页面
// 核心代码
	resp, err := http.Get("http://wwww.baidu.com")
	body, err := ioutil.ReadAll(resp.Body)
	(string(body))		

```

```go
func main() {
    // 指定网页
    url := "http://www.baidu.com/"
    // 下载网页
    download(url)
    
}

func download(url string) resp.Body  {
	// 请求
    req, _ := http.NewRuest("GET",url,nil)
    // 自定义 Header
    req.Header.Set("User-Agent", "Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)")
    
    // 发送请求，接收响应
    client := &http.Client{}
    resp, err := clinet.Do(req)
    if err != nil {
        fmt.Println("http get err",err)
        return nil
    }
    defer resp.Body.Close()
    
    // response
    body, err := ioutil.ReadAll(resp.Body)
    if err != nil {
        fmt.Println("read response err",err)
        return nil
    }
    return resp.Body 
}
```



















































