```c++
template <class T>
void printArr(T arr[], int size) {
    for (int i=0; i<size; i++) {
        cout << arr[i] << "  ";
    }
};

template <class T>
void mySwap(T& a, T& b) {
    T temp = a;
    a = b;
    b = temp;
};
```

