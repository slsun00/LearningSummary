


## 微服务项目
```java
一级项目            后续目录            项目                    
service            


二级项目
    serviec_edu  
    service_oss
    service_sms

固定目录
    src/main/java 
    src/main/test

项目
    com.xxx.xxx.service.edu


// 主要就是下面的需要进行挑选，根据字段生成不同的代码
// 要有默认
// 要一一对应， 有一定的对应规则
// 可视化操作    

项目子目录
    controller/ entity/ feign / listener/ mapper


数据库

api 路由


## 反思：
// 这只是项目， 先生成模板再说吧， 
// 先把基本结构搭起来， 然后做出来基础的模型了，再说其他的，现在这不是重点
// 现在需要先进行 mp 代码的生成，然后再进行自身的创新，没必要现在就做！！！！




    1. 是否可以给一个模板，根据模板进行生成
    2. 最好是进行链式编程： 
        可视化操作：是防止操作过多，没必要一直重复不同的代码
        链式编程： 不用进行可视化打开，直接配置一次，完全打开

    3. 按道理来说，分页也是固定的代码
        只是分页之中需要进行的逻辑业务是不同的， 
        需要进行查询的方式也是不同的
        所以：规范好分页的模式，是不是可以进行规模化开发？？？

        对于联合查询的，因为需要尽量避免jion 查询了，
        name就可以使用一种比较好的方式进行构建， 进行模式化的开发

    4. 是小项目的快速构建，大项目肯定不是一个人在做， 所以就算是轮到你来做什么东西，还是会拆解为小项目，

    5. 增加可编辑窗口， 毕竟不可可能所有的东西都会生成
        貌似也没必要，你生成代码，在 Idea 中刷新，然后 软件会自动生成一个错误标识(强制标识)，你直接使用 f2 定位过去即可，然后在 idea 中尽心修改

    6. 分步生成， 可以逐步动态生成
        毕竟有些人不喜欢全部生成，这样比较容易打断思路

    7. 感觉要模仿java进行数据库的操作
        就模仿 mp 的操作吧， 先读懂 mp 的自动代码生生成器，然后U做成一个以 vue 为基础的前端界面，        

    8. 没必要面面俱到， 可以先让设定一个前提
        比如先创建一个 maven 项目， 该 maven 项目存在的情况下，才能在该项目中进行项目自动构建

    9. 还是的链接数据库，链接了数据可才能自动生成代码， 这个是必须的

    10. 对于接口 url 自动生成，
        1. 可以编写一个处理 url 的程序，提取出来所有的url,
        2. 提取出来所有的 URL 后会有一个选项下拉框，来提供给人们选择对应的 url
        3. 对于 url 的分类、只能提取、智能提示
        4. 可以不控制生成 url 对应的 方法，对于方法可以之后用到了再来进行新增
    
    11 一定要让页面进行实时刷新，具体怎么能做到页面刷新
        但是不会影响到前面页面的开发呢？
        // 对于已经生成的功能，生成的代码，直接默认不能覆盖，不然不知道谁手残一下， 就会把原本生成好的代码插入一些东西，

        或者尝试开启日志？？？？
        把进行操作的所有步骤都记录下来？？ 


        不覆盖，直接进行操作， 貌似也是个好办法？？？？
    12 权限锁定
        长时间不进行操作，默认不能操作， 防止误碰，
        可以设置一个权限操作时间， 对长时间不进行操作， 就将屏幕锁定
        设置快捷键锁定，快捷键解开， 默认不开启

        所著的时候不耽误看界面，但是不能进行操作，

    13 对于一些特定的比较固定的业务，就单独设置把，这样比较好
        业务单独设置开来比较好，可以设置继承前面的目录设置，这样比较好把
        业务分离开来，比较好


    14 等下需要重新学习一下 vue 进行， 然后在进行项目的创建把，
        vue  感觉全部忘记了        

    15 特别对于 什么 微信登录接口、微博登陆接口、
        aliyun oss vod 都是固定的，没必要每次都写好多东西
        这些直接输入然后自动生成就行了，对于需要的参数，设置一个列表，
        然后设置在合适的地方一个文件夹就行了，有更多的时间来阅读文档和测试文档是最好的， 但是避免在一些特别细小的代码上浪费时间去生成代码

    16 感觉可以根据 api 的某种形式记性标记，
        但是还是先做出来一个小小的雏形把，想那么多有啥用呢，东西不落地，还是百搭啊， 落地式最重要的！！！！        

```