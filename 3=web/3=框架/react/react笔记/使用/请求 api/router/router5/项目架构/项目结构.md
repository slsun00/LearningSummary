```js
src
	components
    	一般组件
        	<Deom />
	pages
    	路由组件
        	<Route path="/about" component={Demo}
        
接收到的 props 不同
	一般组件： 
    	组件标签传递的是什么，就能接收到什么
    路由组件：
    	接收三个固定组件
        history:
            go: f go(n)
            goBack: f goBack()
            goForward: f goForward()
            push: f push(path, state)
            replace: f replace(path，state)
        location:
            pathname: "/about"
            search:
            state: undefined
        match:
            params: 0.
            path: "/about"
            url: "/about"


    
```

## 问题

### 样式丢失

```js
样式源码
	<link rel="stylesheet" href="./css/bootstrap.css">
方式一
	<link rel="stylesheet" href="/css/bootstrap.css">  // 常用
方式二
	<link rel="stylesheet" href="%PUBLIC_URL%/css/bootstrap.css">  // react 专用
方式三
	使用 hashRouter  
        
```



