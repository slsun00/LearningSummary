

# 请求报文

## 请求行

### 请求方法

```js
// http 动词
// 使用标准的 Http 方法 具体情况不一样
常用的HTTP动词有下面五个（括号里是对应的SQL命令）。
    GET		（SELECT）：(查询)	从服务器取出资源（一项或多项）。
    POST	（CREATE）：(新建)	在服务器新建一个资源。
    PUT		（UPDATE）：(更新，修改，完整替换)	在服务器更新资源（客户端提供改变后的完整资源）。
    DELETE	（DELETE）：(删除)	从服务器删除资源。
------------------------------------------------------
    PATCH	（UPDATE）：(修改，局部修改)	在服务器更新资源（客户端提供改变的属性）。
    HEAD：	获取资源的元数据。
    OPTIONS：获取信息，关于资源的哪些属性是客户端可以改变的。


    
普通的
    get("/book",...)		// 查询
    get("/create_book",...)  // 创建
    get("/update_book",...)  // 更新
    get("/delete_book",...)  // 删除


使用restful
    GET("/book",...)	// 查询
    POST("/book",...)	// 创建
    PUT("/book",...)	// 更新
    DELETE("/book",...)	 // 删除
```

### 请求路径

#### 请求路径

```js
// 路径表示api的具体网址，网址代表一种资源，
// 网址中不能使用动词，只能使用名词，而其必须是复数

返回商品列表
GET    http://www.example.com/goods

返回单个商品
GET    http://www.example.com/goods/cup

返回新生成的商品
POST   http://www.example.com/goods

返回一个空文档
DELETE http://www.example.com/goods
```

#### 域名

```js
// 应该尽量将API部署在专用域名之下
	https://api.example.com
// 如果确定API很简单，不会有进一步扩展，可以考虑放在主域名下
// 一般将 api 放在组域名下
	http://www.baidu.com/api/
```

#### 版本 

```js
// 一般把接口的版本号放在 url 中
http://www.baidu.com/api/1.0/info
http://www.baidu.com/api/1.2/info
```



### 协议

```js
API与用户的通信协议，总是使用HTTPs协议。
```



# 响应报文

## 状态行

### 状态码 + 描述

```js
// 常见状态码


200 OK - [GET]：服务器成功返回用户请求的数据，该操作是幂等的（Idempotent）。
201 CREATED - [POST/PUT/PATCH]：用户新建或修改数据成功。
202 Accepted - [*]：表示一个请求已经进入后台排队（异步任务）
204 NO CONTENT - [DELETE]：用户删除数据成功。


400 INVALID REQUEST - [POST/PUT/PATCH]：用户发出的请求有错误，服务器没有进行新建或修改数据的操作，该操作是幂等的。
401 Unauthorized - [*]：表示用户没有权限（令牌、用户名、密码错误）。
403 Forbidden - [*] 表示用户得到授权（与401错误相对），但是访问是被禁止的。
404 NOT FOUND - [*]：用户发出的请求针对的是不存在的记录，服务器没有进行操作，该操作是幂等的。
406 Not Acceptable - [GET]：用户请求的格式不可得（比如用户请求JSON格式，但是只有XML格式）。
410 Gone -[GET]：用户请求的资源被永久删除，且不会再得到的。
422 Unprocesable entity - [POST/PUT/PATCH] 当创建一个对象时，发生一个验证错误。


500 INTERNAL SERVER ERROR - [*]：服务器发生错误，用户将无法判断发出的请求是否成功。


```

## 响应头

## 响应体

### 错误信息

```js
// 一般来说，服务器返回的错误信息，以键值对的形式返回
返回的信息中将error作为键名，出错信息作为键值即
```

### 返回结果

```js
服务器返回的数据格式，应该尽量使用JSON，避免使用XML
针对不同操作，服务器向用户返回的结果应该符合以下规范。
    GET /collection：返回资源对象的列表（数组）
    GET /collection/resource：返回单个资源对象
    POST /collection：返回新生成的资源对象
    PUT /collection/resource：返回完整的资源对象
    PATCH /collection/resource：返回完整的资源对象
    DELETE /collection/resource：返回一个空文档

```









## 未整理

```js
使用链接链接相关资源，在返回响应结果时提供链接其他API的方法，使客户端很方便的获取相关联的信息。
```

# 例子

```java
GET：用于获取资源
POST：用于新建资源
PUT：用于更新资源
DELETE：用于删除资源  

例如：
/user/1    GET ：       得到 id = 1 的 user
/user/1    DELETE：  删除 id = 1 的 user
/user/1    PUT：       更新 id = 1 的 user
/user       POST：      新增 user

```







