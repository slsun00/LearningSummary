```go
https://www.liwenzhou.com/posts/Go/Gin_framework/

多个文件进行渲染
1. 模板拆分
	不同的模板放在不同的文件夹中，然后进行解析；也可以不使用，但是推荐使用
	
	一个星星是文件，两个是当前文件夹的所有目录
	使用 r.LoadHTMLGlob("templates/**/*") //加载templates 下的所有文件夹
	批量的时候，r.Get 是需要分别写出来的

2. 直接使用 
	define 直接强制定义要分析的模板


gin 中给模板添加自定义函数 
r.SetFunMap(template.
            
            
            
静态文件
             html 页面上用到的样式文件  。css js 文件
            
            
            
下载个前端模板
将里面的 index 文件放在你的 模板文件家中
然后 get 就可以从这个模板文件夹中读取了       
            
            

            
package main

import (
	"net/http"

	"github.com/gin-gonic/gin"
)

func main() {
	r := gin.Default()

	// Gin框架中使用LoadHTMLGlob()或者LoadHTMLFiles()方法
	// 进行HTML模板解析。
	r.LoadHTMLFiles("templates/index.tmpl")
	r.GET("/json", func(c *gin.Context) {
		
		// // 方法一
		// data := map[string]interface{}{
		// 	"name" : "小王子",
		// 	"message" : "hello world!",
		// 	"age": 18,
		// }

		// 方法二
		data := gin.H{"name":"小王子","message":"hello world","age":18}

		// 方法三 ：结构体
		var msg struct {
			Name string
			Message string
			Age int
		}


		c.JSON(http.StatusOK,data)
	})

	// 启动server
	r.Run(":9090")
}
            
            
```



