## 分割链表

```java
一个链表和一个特定值x，请你对链表进行分隔，
    使得所有小于x的节点都出现在大 于或等于x的节点之前。
// 示例
    输入：head = 1 ->4 ->3 ->2 ->5 ->2 , x = 3 
    输出：1 ->2 ->2 ->4 ->3 ->5    
// 思路
方式一
    构造两个链表
    	链表一： 存放比标准值小的值
    	链表二： 存放比标准值大的值
    	最后： 将两个链表连接起来
方式二
    双指针快排
    	
    	
```

