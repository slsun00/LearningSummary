## 压缩

### 三元

```java
// 本质就是 []{x,y,data} 进行压缩的

// 未完成，需要再进行补充

package main

import "fmt"

/*
压缩数组
1 0 0
0 0 5
3 0 0
矩阵从(1,1)开始记录位置的
*/
type Node struct {
	x    int         // x坐标
	y    int         // y坐标
	data interface{} // 数据
}

type TSMatrix struct {
	x, y int    // 数组规模
	node []Node // 数组
}

func compress(s [3][3]int) {
	for i:= 0; i
}

// 创建压缩数组数组规模
func CreateTsMatrix(rows, columns int) *TSMatrix {
	return &TSMatrix{
		x:    rows,
		y:    columns,
		node: nil,
	}
}

func main() {
	//创建一个二维数组，进行初始化
	var s [3][3]int
	s[0][0] = 1
	s[1][2] = 5
	s[2][0] = 3
	fmt.Println(s)
}

```

