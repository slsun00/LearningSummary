## 案例一

### 公众号

```java
public interface subject {
    
    // 添加用户
    public void addUser(Observer observer)；
        
    
    // 通知用户
    public void notify(String message);
        
}


public class ConcreteSubject {
    private List<Observer observer> weixinUserList = new ArrayList<Observer>();
    
    // 添加用户
    @Override
    public void addUser(Observer observer) {
        this.weixinUserList.add(observer);
    }
    
    // 通知用户
    @Override
    public void notify(String message) {
        for(Observer observer:weixinUserList) {
            observer.notify(message);
        }
    }
}
```

### 微信用户

```java
public interface Observer {
   void update(String message);
}

public class WeixinUser implements Observer {
    private String name;
    //set get 方法
    WeixinUser(String name) {
        this.name = name;
    }
    @Override
    public void update(String message) {
        System.out.Println(name + "发送信息是" + message);
    }
}
```

### 测试运行

```java
public class Main {
    public static void main(String[] args) {
        
        // 创建公众号
        ConcreteSubject subject = new ConcreteSubject();
        
        //创建微信用户
        WeixinUser user1=new WeixinUser("孙悟空");
        WeixinUser user2=new WeixinUser("猪悟能");
        WeixinUser user3=new WeixinUser("沙悟净");
        
        // 添加用户，用于发布信息
        subject.addUser(user1);
        subject.adduser(user2);
        subject.adduser(user3);
        
        // 发布消息
        subject.notify("五一节是个坑")；
    }
}
```

