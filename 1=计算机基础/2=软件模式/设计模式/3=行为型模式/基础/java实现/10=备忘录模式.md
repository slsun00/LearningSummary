## 白箱备忘录

### 发起人

```java
public class role {
    private int vit;  // 生命值
    
    // 初始化
    public void initState() {
        this.vit = 100;
    }
    
    // 状态存储
    public Memento saveState () {
		return RoleStateMemento(ivt);
    }
    
    // 恢复状态
    public void recover(Memento memento) {
        
    } 
    
    public void delete() {
        this.vit--;
    }
}
```



### 备忘录

```java
public interface Memento {}

public class roleMemento implements Memento {
    private int vit;

    // get set 方法省略不写了
    public roleMemento(int vit) {
        this.vit = vit;
    }
}
```



### 备忘录管理者

```java
public class RoleStateCaretaker {
    
    // 也可以设置一个集合
    // private List<Memento> mementoLit;
    private Memento memento;
    
    public Menento getMemento() {
		return this.memento;
    }
    
    public void setMemento(Memento memento) {
        this.memento = memento;
    }
}
```

### 测试

```java
public class Main{
	public static void Main(String[] args) {
        Role role = new Role();
        role.initState();
        
        // 存储当前状态
        Memento sate1 = role.saveMemento();
        
        
        // 管理者进行管理
        RoleStateCaretaker taker = new RoleStateCaretaker();
        taker.setMemento(state1);
        
        // 当前减少
        role.delete();
        
        // 状态恢复
        role.recover(taker.getMemento());
    }
}    

```





## 黑箱备忘录

### 发起人

```java
public class role {
    private int vit;  // 生命值
    
    // 初始化
    public void initState() {
        this.vit = 100;
    }
    
    // 状态存储
    public Memento saveState () {
		return RoleStateMemento(ivt);
    }
    
    // 恢复状态
    public void recover(Memento memento) {
        
    }
    
    // 设置为内部类
    public class roleMemento implements Memento {
        private int vit;

        // get set 方法省略不写了
        public roleMemento(int vit) {
            this.vit = vit;
        }
    }													
    
}
```



### 备忘录

```java
public interface Memento {}
```



### 备忘录管理者

```java
public class RoleStateCaretaker {
    
    // 也可以设置一个集合
    // private List<Memento> mementoLit;
    private Memento memento;
    
    public Menento getMemento() {
		return this.memento;
    }
    
    public void setMemento(Memento memento) {
        this.memento = memento;
    }
}
```

### 测试

```java
和白想备忘录是一致的
```



