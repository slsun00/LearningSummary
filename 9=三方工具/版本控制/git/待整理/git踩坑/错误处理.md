## github 连接尝试

```java
//cmd
	ping github.com
// git: 二选一
	ssh -T git@github.com 
     ssh -V git@github.com        
        
结果
        
```

## E325: 注意 发现交换文件 ".config.swp"

```java
linux服务器里面编辑某个文件时，由于某种原因导致异常退出的时候，再次进入编辑会提示：
/*
    Swap file ".config.swp" already exists!
    [O]pen Read-Only, (E)dit anyway, (R)ecover, (Q)uit, (A)bort:
*/    
注意
    1. 无论你选择哪一个选项，下次进入依然会出现这样的提示
    2. 异常退出后，linux会针对这个文件生成一个swp文件，下次再次编辑会判断如果有这个文件就会依然提示
    3. 如果你没有要还原旧文件内容的打算，就直接把这个文件删除，提示就不会再出现了。
删除命令
    // 在当前目录下面执行ll命令是看不见这个文件的，可以直接执行命令：
    // 后面的是你存在的文件的名字，即要删除的文件
    rm -rf .config.swp
```



## Failed to connect to github.com port 443: Timed out

```java
多是代理问题，你电脑设置了代理，但是 git 中没有配置代理
    设置全局代理
    git config --global http.proxy 172.17.18.80:8080

    查看是否成功
    git config --get http.proxy
    172.17.18.80:8080
    
        
// 项目单独设置
只对 github 的 clone 仓库配置：
        进入 github clone 仓库，运行 git config --local http.proxy 192.168.4.12:8080

        //  .git/config 文件中多了最下面两行
        [http]
        proxy = 10.xx.xx.xx:8080

	这也意味着，我们直接按上面改这个配置文件也能达到同样的目的。
     
```

