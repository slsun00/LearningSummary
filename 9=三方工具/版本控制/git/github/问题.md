# 删除

## 文件

### 本地仓库删除

```css
git rm 文件名称
git rm -r 文件夹/


git rm <file>

// 添加参数
-f 
    强制删除
    删除之前有修改过并且已经放到暂存区域操作
--cached
    文件从暂存区域移除，但工作区保留
    即仅是从跟踪清单中删除
-r
    git rm -r // 会删除执行语句目录下的所有文件和子目录
```



###  github 库

*   删除文件

    ```css
    1. 在 github 直接可以删除
    2.  仅仅删除远程分支文件，不删除本地文件
        git rm --cached filename
        git commit -m "delete remote file filename "
        git push -u origin master(此处是当前分支的名字)
    3. 删除本地文件与远程分支文件
    	git rm filename
        git commit -m "delete file filename "
        git push -u origin master(此处是当前分支的名字)
    ```

*   删除文件夹

    ```css
     /* 在github上只能删除仓库,却无法删除文件夹, 所以只能通过命令来解决 */
    方法一
    	将分支拉下来。删除该文件夹，然后提交到远程库，
    	代码
    		$ git pull origin master                    # 将远程仓库里面的项目拉下来
    		$ dir                                       # 查看有哪些文件夹
    		$ git rm -r --cached target                 # 删除target文件夹
    		$ git commit -m '删除了target'               # 提交,添加操作说明		
    		$ git push -u origin master                 # 将本次更改更新到github项目上去
    	优势
    		1. 地项目中的target文件夹不受操作影响,删除的只是远程仓库中的target, 可放心删除
    
    
    方法二 ：本地和远程都删除
    	本地仓库删除要删除的文件，重新 add commit push 就可以了
    	git rm -r directoryname
        git commit -m "delete directory directoryname "
        git push -u origin master(此处是当前分支的名字)
    
    
    
    
    
    
    方法一
    	/*可以单独创建一个文件夹，用来删除，这个弊端就是会把本地库文件也删除了*/ 
        1.  直接拉取下来 ， 本地库删除文件夹 ： git rm  -r 我的文件/
                    -r表示递归所有子目录，如果你要删除的，是空的文件夹，此处可以不用带上-r。
        2. 提交一个空的上去，不做改变，直接推上去	
    方法二
    	
    ```

## 分支

### 本地库

远程库

```go

首先进入你的master文件夹下, Git Bash Here ,打开命令窗口
$ git pull origin master                    # 将远程仓库里面的项目拉下来
$ dir                                                # 查看有哪些文件夹
$ git rm -r --cached target              # 删除target文件夹
$ git commit -m '删除了target'        # 提交,添加操作说明
$ git push -u origin master               # 将本次更改更新到github项目上去
```



## 库

### 远程库

```css
方法一 ：暂时性删除
	git remote rm pd(远程库别名)
        删除远程库 pd , 其实就是删除了这个别名对应的分支，该别名不没有用了，远程库内容没响
        就是删除远程库在本地的分支（别名）连接

方法二：物理性删除
	1. 进入要删除的库 -  code 哪一行的最右边有个 setting ,  options - 拉倒最下面
	2. Danger Zone  : 中第四个 Delete this repository 
```

# 拉取

## 拉取某一个分支的指定文件、文件夹

```css
方法一
	1.core.sparsecheckout用于控制是否允许设置pull指定文件/夹，true为允许。
		$ git config core.sparsecheckout true
        此方法适用于 Git1.7.0 以后版本，，本质是开启sparse clone
	2. 在.git/info/sparse-checkout文件中（如果没有则创建）添加指定的文件
		echo "fileName" >> .git/info/sparse-checkou
	3. 最后，拉取想要的分支即可实现checkout指定文件/夹。
		$ git pull origin master

    # 3. 开启sparse clone

	# 本地目录的.git文件夹下，如果没有sparse-checkout文件则创建，在其中添加指定的文件/夹fileName，就是需要拉取的那个特定文件/夹。*表示所有，！表示匹配相反
    echo "fileName" >> .git/info/sparse-checkout 
    cat .git/info/sparse-chechout # 查看
    # 4. 拉取指定目录
    git pull origin master # 拉取命令是一样的，只是已经通过配置文件sparse-chechout指定了目标文件/夹
	


## step 1 :初始化空库
[root@vm_test backup]# mkdir devops
[root@vm_test backup]# cd devops/
[root@vm_test devops]# git init                  #初始化空库

## step 2 :  拉取remote的all objects信息
[root@vm_test devops]# git remote add -f origin 


## step 3 :  #3.1 开启sparse clone, #3.2 设置需要pull的目录 devlops
# git config core.sparsecheckout true   #开启sparse clone
# echo "devops" >> .git/info/sparse-checkout   #设置需要pull的目录，*表示所有，!表示匹配相反的
# more .git/info/sparse-checkout

## step 4 :  # 将origin 端，由第三步（文件 .git/info/sparse-checkout）设置的 目录下的文件 pull 到本地
[root@vm_test devops]# git pull origin master  
```





# 注意

```css
因为 git 空目录无法add。如果想add一个空目录，则需要在它下面创建一个文件
```



















































































































































































