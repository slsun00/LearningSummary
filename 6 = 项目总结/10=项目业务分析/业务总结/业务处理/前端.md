## 发送请求

```java

标签
link 
	href
a
	href= 提交地址； 默认 get 提交
iframe
	src   // 基本弃用       
script
	src	 get 请求
img
	src
ajax
	url： 提交地址
    type: 提交方法
form
	method: 请求方法
	action： 提交地址
button
	type: submit 提交表单        
```

## 前端页面

### html 转 jsp

```java
1. 将 html 文件中的代码复制到 jsp 页面中
// 修改

2. 引入资源文件到 webapp 文件中
    前端资源文件
	web-inf
    	jsp 文件
    	web.xml
    index.jsp
    
 3. 页面修改
1. 字符集 
    <html lang="UTF-8">
    <meta charset="UTF-8">
2. form 表单
    form 标签： 添加 method  action
    input 标签： 添加 name 属性值, 用于后端识别提交的是谁
    button 标签： type="submit" 否则不会提交表单
```

### 业务

```java
1. 返回上一步
2.     
```

## vue

```java
路由
    <html>
         router-link to //  定义路由
        router- view   
    </html>
	<script>
    	// router 管理 routrs , routers 中有好多路径，每个路径要配置一个组件
		// 相当于 水厂 管理南山区水库 ，南山区水库中有好多输水管, 不同的水管通向不同的城区
    	定义路由s -- 》 路由挂载组件,组件就是需要取得各个界面  -- 
    	管理路由的路由实例s ==》 注册入 dom
    </script>
```

