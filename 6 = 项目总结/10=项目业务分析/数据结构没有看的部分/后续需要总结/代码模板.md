```java
// controller 代码生成器
这种应该是固定的格式，所以可以配置固定的方法模板，
可以自动生成，没必要到时候自动去敲， 这个留着以后思考

@Api(tags = "阿里云文件管理")
@RestController
@CrossOrigin
@RequestMapping("/admin/oss/file")
public class FileController {}
```

