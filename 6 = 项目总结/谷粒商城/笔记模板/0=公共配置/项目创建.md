## == 后端 ==

## java

```java
1. 数据库创建
    创建数据库，在数据库中写入数据，进行数据展示
2. 后端创建
	特别注意
		/*
     	1. 先将 idea project 关闭， 然后进行缓存清除 & 重启
    	2. 即使是重新打开的 idea , 也要进行 缓存清除和重启
    	3. 在创建子项目之前，
    		把依赖配置完全（最起码是单独的依赖）
    		否则：在创建完子项目以后再进行 parent 依赖添加，容易出错
		
		*/
    	1. 与框架相关的配置
    		数据库： 代码生成器
                           
3. 前端创建    
    
```

