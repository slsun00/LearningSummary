```java
package com.boyanTest.controller;

import com.boyanTest.common.result.R;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiModelProperty;
import io.swagger.annotations.ApiOperation;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * @author slsun
 * @version 1.0
 * @title: LoginController
 * @description  正常登陆、退出的操纵
 * @date: 2021/7/7 16:28
 */
@RequestMapping("/user")
@CrossOrigin
@Api(tags = "用户登陆(注册)退出(注销)")
public class LoginController {

    // 现在又两个问题， 一个是登陆， 一个是注册
    /*
    *  就是说一个用户进行登陆，后端并不知道他是已有的用户的登陆，还是新用户
    *  所以这里两种情况进行注册处理
    *  1. 用户直接登录
    *       有一个逻辑误区就是，你没有注册，为什么直接去登录呢？
    *       所以在后端默认是：
    *           你登录就是默认你是注册用户， 至于你是不是新用户， 无所谓，
    *            查不到这个账号
    *               1. 就让你一直回到登录界面
    *               2. 在登陆的下面，显示一个找回密码的操作
    *               3. 重复输入几次，需要进行验证
    *
    *  2. 用户直接注册
    * */
    @PostMapping("/login")
    @ApiOperation("登陆")
    public R login() {

    }

}

```

