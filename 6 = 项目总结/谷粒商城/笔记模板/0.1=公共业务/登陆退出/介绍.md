## 登陆流程

```java
前端
    填写完表格，将数据发送给后端

后端
    接收数据
    1. 进行数据验证，验证码验证， 
    2. 生成 token ，然后将 token 发送到 redis , 
		第一种方法： 返回给前端, 让前端进行 cookie 保存， 存在 header 中
		第二种方法： 直接在 request 中设置进行返回， 存在 header 中

```

